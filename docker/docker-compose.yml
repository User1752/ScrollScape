services:
  # ── ScrollScape ───────────────────────────────────────────────────
  scrollscape:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: scrollscape
    ports:
      - "4000:4000"
    volumes:
      - ../data:/app/data
      - ../public:/app/public
    environment:
      - PORT=4000
    restart: unless-stopped
